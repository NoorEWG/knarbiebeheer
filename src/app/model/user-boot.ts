import { Boot } from './boot';
import { User } from './user';

export class UserBoot {
  user: User;
  boat: Boot;
  saveUser: boolean;
  saveBoat: boolean;
  saveAbo: boolean;
  updateUser: boolean;
  updateBoat: boolean;
  updateAbo: boolean;
  year: number;
}
